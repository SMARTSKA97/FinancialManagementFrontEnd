import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ska97.finplanner',
  appName: 'Financial Planner',
  webDir: 'dist/angular-app/browser',
};

export default config;
